# PyBun Agent Guide

This file has moved. See [`AGENTS.md`](AGENTS.md) for the maintained repository guide for AI coding agents (project structure, build/test/lint commands, coding style, commit/PR conventions, release checklist).

Kept only as a redirect so tooling that looks for the singular `AGENT.md` filename still finds guidance; it previously described commands like `install`, `test`, `build`, and `doctor` as "planned" — they have since shipped.
