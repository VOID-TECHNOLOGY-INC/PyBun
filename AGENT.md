# PyBun Agent Guide

This file has moved. See [`AGENTS.md`](AGENTS.md) for the maintained repository guide for AI coding agents (project structure, build/test/lint commands, coding style, commit/PR conventions, release checklist).

This file previously described several commands (`install`, `test`, `build`, `doctor`, `mcp serve`) as "planned" — they have since shipped. It is kept only as a redirect in case tooling looks for the singular `AGENT.md` filename.
